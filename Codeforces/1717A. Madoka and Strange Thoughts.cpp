#include<bits/stdc++.h>
using namespace std;
#define ll long long

#define pb push_back
#define solve(a) sort(a.begin(), a.end()); (a).erase(unique((a).begin(),(a).end()),(a).end())
int main()
{
    int IJR=1,tc=0,Case=0;  cin>>IJR;
    while(IJR--)
    {
        ll n,k,ans=0,c=0,c1=0,i,x=3,j,a,b,d,y,mnh=10000000,mx=0;
        cin>>n;

        ans=n+(n/2)*2+(n/3)*2;



        cout<<ans<<endl;
    }

    return 0;
}

